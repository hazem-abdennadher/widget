// This is an extra step because iconify icon's doesn't have autocomplete for import, with these,
// we can get auto import, and that accelerate the development speed
export { Icon } from "@iconify/react";
